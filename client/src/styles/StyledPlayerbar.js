import styled from "styled-components/macro";

const StyledPlayerbar = styled.header`
  position: sticky;
  display: flex;
  top: 0;
  background-color: #121212;
  min-height: 60px;
  z-index: 1000;
  height: 60px;
  justify-content: center;
`;

export default StyledPlayerbar;
